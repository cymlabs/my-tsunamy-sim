# my-tsunamy-sim

Simple Express wrapper for the Deluge WebGPU demo. Run it locally with:

1. Install dependencies: `npm install`
2. Start the server: `npm start`
3. Visit http://localhost:3000 to interact with the UI.

For automated checks or container environments, hit the health probe at http://localhost:3000/health to confirm the server is running.
